USE DATABASE ynov_ci;
CREATE TABLE utilisateurs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(100),
    email VARCHAR(150),
    age INT,
    date_creation TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
